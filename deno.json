{
  "tasks": {
    "server": "deno run --allow-net --allow-read server.ts",
    "tailwind": "tailwindcss -i ./public/tailwind.css -o ./public/styles.css --watch",
    "dev": "deno task tailwind & deno task server"
  }
}
