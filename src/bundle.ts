// bundle.ts
// import { transpile, TranspileOptions } from "https://deno.land/x/emit/mod.ts";
// import { z } from "https://cdn.jsdelivr.net/npm/zod@3.21.4/+esm";
//
// // Define transpilation options
// const options: TranspileOptions = {
//   compilerOptions: {},
// };

// Read TypeScript code (replace "input.ts" with the actual file)
// const tsCode = await Deno.readTextFile(
//   "https://cdn.jsdelivr.net/npm/zod@3.21.4/+esm",
// );

// Transpile TypeScript to JavaScript
// const transpiledCode = await transpile(z, options);
// console.log(transpiledCode.keys());

// Write the transpiled JavaScript output
// await Deno.writeTextFile("output.js", transpiledCode);
